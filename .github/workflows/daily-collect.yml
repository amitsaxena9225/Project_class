name: Daily Approval Summary (Today)

on:
  schedule:
    - cron: "* * * * *"   # 9:00 AM IST (03:30 UTC)
  workflow_dispatch:

permissions:
  issues: write
  contents: read

jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - name: Create today’s approval summary
        uses: actions/github-script@v7
        with:
          script: |
            const APPROVER = "amitsaxena9225"; // 👈 your GitHub username

            // ---- IST start of today → UTC ----
            const istOffset = 330; // minutes
            const now = new Date();
            const istNow = new Date(now.getTime() + istOffset * 60000);
            istNow.setHours(0, 0, 0, 0);

            const sinceUTC = new Date(istNow.getTime() - istOffset * 60000)
              .toISOString();

            const todayStr = istNow.toISOString().split("T")[0];

            // ---- Prevent duplicate summary ----
            const existing = await github.rest.search.issuesAndPullRequests({
              q: `repo:${context.repo.owner}/${context.repo.repo} 
                  in:title "Daily Approval Summary – ${todayStr}"`,
            });

            if (existing.data.items.length > 0) {
              console.log("Summary already exists. Skipping.");
              return;
            }

            // ---- Fetch today's issues ----
            const result = await github.rest.search.issuesAndPullRequests({
              q: `mentions:${APPROVER} is:open created:>=${sinceUTC}`,
              per_page: 100
            });

            const issues = result.data.items;

            if (issues.length === 0) {
              console.log("No tickets created today.");
              return;
            }

            let body = `📌 **Daily Approval Summary – ${todayStr} (IST)**\n\n`;
            body += `Comment **APPROVE-ALL** to approve all tickets below:\n\n`;

            for (const i of issues) {
              body += `• #${i.number} – ${i.title}\n`;
            }

            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `Daily Approval Summary – ${todayStr}`,
              body,
              labels: ["daily-summary"]
            });
